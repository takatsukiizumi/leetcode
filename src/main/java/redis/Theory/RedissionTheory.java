package redis.Theory;

/**
 * @Description:
 * @author: takatsukiizumi
 * @date: 2023/3/16
 */

public class RedissionTheory {
    //Redis支持以下数据结构：
    //1.字符串（string）
    //2.哈希（hash）
    //3.列表（list）
    //4.集合（set）
    //5.有序集合（sorted set）
    //6.位图（bitmap）

    //Redission是基于Redis的Java客户端，可以实现Redis中的所有数据结构的相关操作。
    // 根据官方文档，其提供以下API对应Redis中的数据结构：

    //Redisson object - 提供了原始的redis API的访问能力和更熟悉的常用Java数据结构（如Map，List，Set等）的API，支持分布式锁和并发。
    //RMap - Redis的哈希表实现，支持分布式。
    //RList - Redis的列表实现，支持分布式。
    //RSet - Redis的集合实现，支持分布式。
    //RSortedSet - Redis的有序集合实现，支持分布式。
    //RBucket - Redis的字符串实现，可以用来存储java对象，支持分布式。
    //RBinaryStream - Redis的bit数组实现，支持分布式。
    //RAtomicLong - Redis的原子计数器实现，支持分布式。
    //RLock - Redis的分布式锁实现。
    //RSemaphore - Redis的分布式信号量实现。
    //RCountDownLatch - Redis的分布式计数器实现。
    //RTopic - Redis的发布/订阅实现，支持分布式。
    //RLiveObject - Redis的对象存储实现，支持JPA和POJO。
    //branch1
}
